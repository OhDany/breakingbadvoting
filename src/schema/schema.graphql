type Query {
  characters: [Character!]!
  character(id: ID!): Character
}

type Character {
  id: ID!
  name: String!
  actor: String!
  description: String!
  total_episodes: Int!
  photo: String!
  votes: Int!
  url: String!
}
